#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-thumbnail/bin/thumb" "$@"
  ret=$?
else 
  node  "$basedir/../node-thumbnail/bin/thumb" "$@"
  ret=$?
fi
exit $ret
